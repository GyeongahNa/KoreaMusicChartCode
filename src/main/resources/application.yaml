spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ********
    username: ********
    password: ********

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 1000

logging:
  level:
    org.hibernate.SQL: debug